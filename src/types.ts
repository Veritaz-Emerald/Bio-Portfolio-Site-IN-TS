export interface SocialLink {
  id: string;
  name: string;
  url: string;
  icon: string;
  enabled: boolean;
}

export interface Song {
  title: string;
  artist: string;
  url: string;
}

/* CBV Signature; VmVyaXRheg */